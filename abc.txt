<script>
    window.contextPath = "${pageContext.request.contextPath}";
    window.categoryMap = {
        <%
            java.util.Map<?, ?> catMap = (java.util.Map<?, ?>) request.getAttribute("categoryMap");
            if (catMap != null) {
                java.util.Iterator<?> it = catMap.entrySet().iterator();
                while (it.hasNext()) {
                    java.util.Map.Entry<?, ?> entry = (java.util.Map.Entry<?, ?>) it.next();
                    out.print("\"" + entry.getKey() + "\": \"" + entry.getValue() + "\"");
                    if (it.hasNext()) {
                        out.print(",");
                    }
                }
            }
        %>
    };
</script>

