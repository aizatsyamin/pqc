document.addEventListener("DOMContentLoaded", () => {
    // Select all the boxes within the specific container
    const fpBoxes = document.querySelectorAll('.fp-box');

    // Add a click event listener to each box to toggle selection
    fpBoxes.forEach(box => {
        box.addEventListener('click', () => {
            box.classList.toggle('selected');
        });
    });
});
